#include <stdio.h>
#include <stdlib.h>
#include <ctype.h> /* for isdigit() */
#include <math.h> /* for sin, exp, pow, etc. */
#include <string.h>
#include "stack.h"

void push(double);
double pop(_Bool idntt);

double ans;
